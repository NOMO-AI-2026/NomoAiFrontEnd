# Production build (Vercel / static host) — browser calls MonsterASP directly.
# Requires Cors:AllowedOrigins on the API to include this frontend origin.
VITE_API_BASE_URL=https://nomoai.runasp.net/api
